package com.oracle;

import java.io.File;
import java.io.FileWriter;
import java.io.IOException;

public class TestInputArgs {

	/**
	 * @param args
	 * @throws IOException 
	 */
	public static void main(String[] args)  {
		// TODO Auto-generated method stub
		
		
		for(int i = 0; i < args.length; i ++){
			System.out.println(args[i]);
		}
		try {
			File c  =  new File("d:\\");
			File f =  new File(c, "1.txt");
			System.out.println(f.getParent());
			if(!f.exists())
			f.createNewFile();
			FileWriter writer =  new FileWriter(f, true);
			writer.write("xxxxxxxxxx");
			writer.flush();
			writer.close();
			
			String ldapUrl = "ldap://10.245.28.45:9234";
			 ldapUrl = ldapUrl.substring(7);
			System.out.println(ldapUrl); 
			String host =  ldapUrl.substring(0, ldapUrl.indexOf(":"));
			ldapUrl = ldapUrl.substring(ldapUrl.indexOf(":")+1);
			System.out.println(ldapUrl); 
			
		
				
			
		} catch (IOException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
	}

}
